/**
 * HeaderBar Component Wrapper for Home
 * Reuses the existing HeaderBar from the booking flow as per requirements.
 */

import HeaderBar from '@/components/BookService/HeaderBar';

export default HeaderBar;
